class Solution {
public:
    int countCommas(int n) {
        int copy=n;
        string s = to_string(copy);
        int m = s.size();
        int sub = pow(10,m-2);
        if(m<4) return 0;
        return n-999;
    }
};